name: Ping Render Service

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the URL
        run: |
          curl -I "${{ secrets.RENDER_URL }}"
